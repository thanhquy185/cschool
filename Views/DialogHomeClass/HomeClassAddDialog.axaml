<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:cschool.ViewModels"
        x:DataType="vm:HomeClassViewModel"
        x:Class="cschool.Views.DialogHomeClass.HomeClassAddDialog"
        Title="Cập nhật hạnh kiểm"
        Width="400"
        Height="350"
        WindowStartupLocation="CenterOwner"
        CornerRadius="8"
        Padding="0"
        SizeToContent="Manual"
        CanResize="False">

    <Border CornerRadius="8" Background="White" BorderBrush="#DDD" BorderThickness="1">
        <Grid RowDefinitions="Auto,*,Auto">
            
            <!-- Header - MÀU XANH LÁ CÂY (Giống hệt DetailDialog) -->
            <Border Background="#4CAF50" Padding="16,12" CornerRadius="8,8,0,0">
                <Grid>
                    <TextBlock Text="CẬP NHẬT HẠNH KIỂM"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Nội dung -->
            <Border Grid.Row="1" Padding="20">
                <StackPanel Spacing="15">
                    <!-- Học sinh -->
                    <Border BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            Background="#F9F9F9">
                        <Grid ColumnDefinitions="Auto,*">
                            <TextBlock Text="Học sinh:"
                                       FontWeight="SemiBold"
                                       Foreground="#333"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding SelectedStudent.StudentName}"
                                       FontWeight="Bold"
                                       Foreground="#4CAF50"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Hạnh kiểm hiện tại -->
                    <Border BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            Background="#FFF8E1">
                        <Grid ColumnDefinitions="Auto,*">
                            <TextBlock Text="Hạnh kiểm hiện tại:"
                                       FontWeight="SemiBold"
                                       Foreground="#333"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding SelectedStudent.ConductLevel, FallbackValue='Chưa có'}"
                                       FontWeight="SemiBold"
                                       Foreground="#F57C00"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Chọn hạnh kiểm mới -->
                    <StackPanel>
                        <TextBlock Text="Chọn hạnh kiểm mới:"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Margin="0,0,0,8"/>
                        
                        <ComboBox ItemsSource="{Binding ConductOptions}"
                                  SelectedItem="{Binding SelectedConductLevel}"
                                  FontSize="14"
                                  Height="40"
                                  BorderThickness="1"
                                  BorderBrush="#CCCCCC"
                                  CornerRadius="4"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Nút điều khiển - Giống với DetailDialog -->
            <Border Grid.Row="2"
                    Padding="0,15,0,20"
                    CornerRadius="0,0,8,8"
                    Background="White">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                    <Button Content="💾 Lưu" 
                            Background="#4CAF50" 
                            Foreground="White" 
                            Width="120"
                            Height="36"
                            FontSize="12"
                            BorderThickness="0"
                            CornerRadius="4"
                            Command="{Binding SaveConductCommand}"
                            IsEnabled="{Binding SelectedConductLevel, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                    
                    <Button Content="❌ Hủy" 
                            Background="#6c757d" 
                            Foreground="White" 
                            Width="120"
                            Height="36"
                            FontSize="12"
                            BorderThickness="0"
                            CornerRadius="4"
                            Command="{Binding CancelConductCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>