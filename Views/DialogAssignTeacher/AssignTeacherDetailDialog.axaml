<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ViewModels;assembly=ViewModels"
        xmlns:model="using:Models"
        x:DataType="vm:AssignTeacherViewModel"       
        x:Class="Views.DialogAssignTeacher.AssignTeacherDetailDialog"
        Title="Chi tiết phân công"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterOwner"
        CornerRadius="8"
        Padding="0">

    <Border CornerRadius="8" Background="White" BorderBrush="#DDD" BorderThickness="1">
        <Grid RowDefinitions="Auto,*,Auto">
            
            <!-- 🔹 Header -->
            <Border Background="#4CAF50" Padding="16,12" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Tiêu đề ở giữa -->
                    <TextBlock Grid.Column="1"
                            Text="CHI TIẾT PHÂN CÔNG"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="White"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            />
                </Grid>
            </Border>

            <!-- 🔹 Nội dung -->
            <ScrollViewer Grid.Row="1" Padding="25" VerticalScrollBarVisibility="Auto">
                <Grid ColumnDefinitions="*,*" 
                      RowDefinitions="Auto,Auto,Auto,Auto,Auto"
                      ColumnSpacing="20" 
                      RowSpacing="15">

                    <!-- Hàng 1: Giáo viên & Môn học -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Giáo viên:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding SelectedTeacher.Name}" 
                                IsReadOnly="True"
                                Height="35"
                                Width="270"
                                FontSize="14"
                              
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Môn học:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding SelectedSubject.Name_Subject}" 
                                IsReadOnly="True"
                                Height="35"
                                Width="270"
                                FontSize="14"
                              
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <!-- Hàng 2: Lớp học & Ngày học -->
                    <StackPanel Grid.Row="1" Grid.Column="0">
                        <TextBlock Text="Lớp học:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding SelectedClass.Name}" 
                                IsReadOnly="True"
                                Height="35"
                                Width="270"
                                FontSize="14"
                                
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBlock Text="Ngày học:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding SelectedDay}" 
                                IsReadOnly="True"
                                Height="35"
                                Width="270"
                                FontSize="14"
                                
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <!-- Hàng 3: Tiết bắt đầu & Tiết kết thúc -->
                    <StackPanel Grid.Row="2" Grid.Column="0">
                        <TextBlock Text="Tiết bắt đầu:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Start}" 
                                IsReadOnly="True"
                                Height="35"
                                FontSize="14"
                              
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBlock Text="Tiết kết thúc:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding End}" 
                                IsReadOnly="True"
                                Height="35"
                                FontSize="14"
                                
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <!-- Hàng 4: Bài kiểm tra & Bài miệng -->
                    <StackPanel Grid.Row="3" Grid.Column="0">
                        <TextBlock Text="Bài kiểm tra:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding QuizCount}" 
                                IsReadOnly="True"
                                Height="35"
                                FontSize="14"
                                
                                Background="#F5F5F5"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="1">
                        <TextBlock Text="Bài miệng:" FontSize="14" Margin="0,0,0,5" Foreground="Black" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding OralCount}" 
                                IsReadOnly="True"
                                Height="35"
                                FontSize="14"
                                
                                Background="#F5F5F5"/>
                    </StackPanel>

                </Grid>
            </ScrollViewer>

            <!-- 🔹 Nút hành động - Chỉ giữ nút Đóng -->
            <Border Grid.Row="2"
                    Padding="0,15,0,20"
                    CornerRadius="0,0,8,8"
                    Background="White">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Spacing="15">
                    <Button Content="❌ Đóng" 
                            Background="#6c757d" 
                            Foreground="White" 
                            Width="110"
                            Height="36"
                            FontSize="14"
                            BorderThickness="0"
                            CornerRadius="4"
                            x:Name="CloseButton"
                            Click="OnCancelButtonClick"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>