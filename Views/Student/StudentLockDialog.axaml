<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:cschool.ViewModels;assembly=cschool"
        x:Class="cschool.Views.Student.StudentLockDialog"
        x:DataType="vm:StudentViewModel"
        Title="Khoá người dùng"
        Width="400" Height="250"
        WindowStartupLocation="CenterOwner">

    <!-- <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">
        <Image Width="60" Height="60" Source="{Binding Icon}" />
        <TextBlock Text="{Binding Message}" TextAlignment="Center" FontSize="16"/>
        <Button Content="Xác nhận" Command="{Binding ConfirmCommand}" Width="150" />
    </StackPanel> -->
</Window>